# Cloudinary — powers the photography system (/photos, journal photos, hero images).
# Without this, image components render labelled placeholders so the build still works.
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name
